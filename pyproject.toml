[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openenv-environment"
version = "1.0.0"
description = "Hackathon OpenEnv Submission"
dependencies = [
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:app"